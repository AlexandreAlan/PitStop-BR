# Hardening Apache — PitStop BR

ServerTokens Prod
ServerSignature Off
TraceEnable Off

<Directory /var/www/html>
    Options -Indexes -FollowSymLinks
    AllowOverride None
    Require all granted
</Directory>

# Bloqueia acesso a arquivos sensíveis caso sejam copiados por engano para o docroot
<FilesMatch "^\.|\.(env|sql|ini|log|md|yml|yaml)$">
    Require all denied
</FilesMatch>

<IfModule mod_headers.c>
    Header always set X-Content-Type-Options "nosniff"
    Header always set X-Frame-Options "DENY"
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
    Header always set Permissions-Policy "geolocation=(), camera=(), microphone=()"
    Header always set Content-Security-Policy "default-src 'self'; connect-src 'self' https://cdn.jsdelivr.net; script-src 'self' https://cdn.jsdelivr.net; style-src 'self' https://cdn.jsdelivr.net 'unsafe-inline'; font-src 'self' https://cdn.jsdelivr.net; img-src 'self' data:; object-src 'none'; base-uri 'self'; frame-ancestors 'none'"
    # HSTS: essa app só existe em produção atrás de HTTPS (o :80 no nginx só
    # redireciona pra :443, nunca chega no Apache em texto puro) — força o
    # navegador a nunca mais tentar http:// puro pra esse domínio, fechando
    # a janela de downgrade/SSL-stripping na primeira visita de cada usuário.
    Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains"
    Header unset X-Powered-By
</IfModule>
